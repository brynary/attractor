Read all product docs under docs-internal/product/, all architecture docs under docs-internal/architecture/, and the current codebase.

The requested change: $goal

Analyze how this change affects the existing product:
- Does it require new features, modifications to existing features, or removal of features?
- Which existing features are affected?
- What are the dependencies and ripple effects?
- Are there conflicts with existing requirements or architecture?
